import styled from '@emotion/styled';

const StyledPage = styled.div`
  .page {
  }
`;

export function TimeTablePage() {
  return <StyledPage>タイムテーブルをいじるページ</StyledPage>;
}

export default TimeTablePage;
