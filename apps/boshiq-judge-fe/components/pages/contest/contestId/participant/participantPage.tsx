import styled from '@emotion/styled';

const StyledPage = styled.div`
  .page {
  }
`;

export function ParticipantPage() {
  return <StyledPage>出場者一覧</StyledPage>;
}

export default ParticipantPage;
