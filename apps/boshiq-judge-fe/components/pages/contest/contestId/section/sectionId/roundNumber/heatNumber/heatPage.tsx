import styled from '@emotion/styled';

const StyledPage = styled.div`
  .page {
  }
`;

export function HeatPage() {
  return <StyledPage>ジャッジをしたり結果を見たりするページ</StyledPage>;
}

export default HeatPage;
