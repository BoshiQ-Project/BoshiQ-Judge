import styled from '@emotion/styled';

const StyledPage = styled.div`
  .page {
  }
`;

export function NewContestPage() {
  return <StyledPage>名前と日付と開催場所を決める。</StyledPage>;
}

export default NewContestPage;
